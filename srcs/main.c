#include <fcntl.h> 
#include<ft.h>
#include<stdio.h>
#include "ft.h"

int main(int argc, char** argv) 
{ 

    FILE *fp;
    char ch numbers.dict.txt[1024]; 
    fp = fopen("numbers.dict.txt", "r");

    fclose(fp);

struct number
{
    char *key;
    char *value;
    struct number *next;
};





}
